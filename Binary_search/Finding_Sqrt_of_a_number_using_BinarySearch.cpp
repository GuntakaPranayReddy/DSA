#include<bits/stdc++.h>
using namespace std;
int main()
{  
    int n;
    cin>>n;
    int p=sqrt(n);
    cout<<p<<endl;
    //time complexity O(log n) and space complexity O(1).
   // brute force method is ro take 1 to n and by applying for loop and get the results.time complexity O(n)
}